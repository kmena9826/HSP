@model IEnumerable<CS_HOSPITALARIO.Models.CS_PACIENTES>

@{
    ViewBag.Title = "Pacientes";
}

<h2>@ViewBag.Title</h2>
<hr />

<table class="table table-hover" id="tabla_pacientes">
    <thead>
        <tr>
            <th>
                ID
            </th>
            <th>
                Nombres
            </th>
            <th>
                Apellidos
            </th>
            <th>
                Identificación
            </th>
            <th>
                Seguro Social
            </th>
            <th>
                Fecha Registro
            </th>
        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CLIENTE_ID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NOMBRES)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.APELLIDOS)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IDENTIFICACION)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NUM_SEGURO_SOCIAL)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FECHA_REGISTRO)
                </td>
            </tr>
        }

    </tbody>
    <tfoot>
        <tr>
            <th>
                ID
            </th>
            <th>
                Nombres
            </th>
            <th>
                Apellidos
            </th>
            <th>
                Identificación
            </th>
            <th>
                Seguro Social
            </th>
            <th>
                Fecha Registro
            </th>
        </tr>
    </tfoot>


</table>

<br />

<link href="~/Content/DataTables/css/PagedList.css" rel="stylesheet" />
<link href="~/Content/DataTables/css/jquery.dataTables.min.css" rel="stylesheet" />
<link href="~/Content/DataTables/css/buttons.dataTables.min.css" rel="stylesheet" />
<link href="~/Content/DataTables/css/DataTableSearch.css" rel="stylesheet" />

@section scripts
{
    <script src="~/Scripts/jquery.dataTables.min.js"></script>
    <script src="~/Scripts/dataTables.buttons.min.js"></script>
    <script src="~/Scripts/buttons.flash.min.js"></script>
    <script src="~/Scripts/jszip.min.js"></script>
    <script src="~/Scripts/buttons.html5.min.js"></script>
    <script src="~/Scripts/dataTables.select.min.js"></script>
    <script src="~/Scripts/pdfmake.min.js"></script>
    <script src="~/Scripts/vfs_fonts.js"></script>

    <script type="text/javascript">

        /* INICIALIZACIÓN */
        $(document).ready(function () {
            $('#tabla_pacientes tfoot th').each(function () {
                var title = $(this).text().trim();
                $(this).html('<input class="form-control" type="text" placeholder="Buscar por ' + title + '" />');
            });

            var events = $('#events');

            var table = $('#tabla_pacientes').DataTable({
                dom: 'Bfrtip',
                language: {
                    select: {
                        rows: {
                            0: "",
                            1: " │ %d registro seleccionado."
                        }
                    }
                },
                select: true,
                buttons: [{
                    text: 'Nuevo',
                    action: function (e, dt, node, config) {
                        window.location.href = '/Admision/NuevoPaciente';
                    },
                    init: function (api, node, config) {
                        $(node).removeClass('btn-default')
                    }
                },
                {
                    text: 'Editar',
                    action: function (e, dt, node, config) {
                        var rowSelected = table.rows({ selected: true }).data();
                        console.log(rowSelected[0][0]);
                        window.location.href = '/Admision/EditarPaciente/' + rowSelected[0][0];
                    },
                    init: function (api, node, config) {
                        $(node).removeClass('btn-default')
                    }
                },
                {
                    text: 'Borrar',
                    action: function (e, dt, node, config) {
                        var rowSelected = table.rows({ selected: true }).data();
                        console.log(rowSelected[0][0]);
                        window.location.href = '/Admision/BorrarPaciente/' + rowSelected[0][0];
                    },
                    init: function (api, node, config) {
                        $(node).removeClass('btn-default')
                    }
                }, "pdf", "excel"
                ]
            });
            // Apply the search
            table.columns().every(function () {
                var that = this;

                $('input', this.footer()).on('keyup change', function () {
                    if (that.search() !== this.value) {
                        that
                            .search(this.value)
                            .draw();
                    }
                });
            });
            $('#tabla_pacientes tfoot tr').appendTo('#tabla_pacientes thead');
            $("#tabla_pacientes_wrapper button").each(function () {
                $(this).removeClass("dt-button");
                $(this).addClass("btn btn-sm");
                if ($(this).text() == 'PDF' || $(this).text() == 'Excel') {
                    $(this).addClass("btn-default");
                }
                else {
                    $(this).addClass("btn-primary ");
                }
            });
            $('#tabla_pacientes_filter input').addClass("form-control");
            $('#tabla_pacientes tbody').on('dblclick', 'tr', function () {
                var rowSelected = table.rows({ selected: true }).data();
                window.location.href = '/Admision/EditarPaciente/' + rowSelected[0][0];
            });
        });

    </script>

}